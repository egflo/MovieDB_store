
import  Search from "./Search"

export default async function Page({
                                       params,
                                   }: {
    params: Promise<{ }>
}) {

    return (
        <Search />
    )
}

