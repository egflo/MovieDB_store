# Copy to local.env and fill in. KEY=value, one per line.
# Real environment variables with the same names take precedence.

# Firebase project the web app signs in to (Project settings > General).
FIREBASE_PROJECT_ID=your-project-id

# Stripe test keys (Dashboard > Developers > API keys).
STRIPE_SECRET=sk_test_...
STRIPE_PUBLIC=pk_test_...
